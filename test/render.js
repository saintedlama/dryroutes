var render = require('../lib/render');
var expect = require('chai').expect;

describe('render', function() {
  it('should call render if view is set', function() {

  });

  it('should not call render if view is not set', function() {

  });
});
